<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>
    <div>
      <h1>
        The current <%= games.length == 1 ? "winner is" : "winners are" %>...
      </h1>

      <% for (let game of games) { %>
      <div class="game-info" votes="<%= game.votes%>">
        <h2>Game: <%= game.title %></h2>
        <h3>With <%= game.votes%> Votes!</h3>
        <!-- <ul>
                <li>Type: <%= game.gameType %></li>
                <li>Minimum Players: <%= game.minPlayers %></li>
                <li>Maximum Players: <%= game.maxPlayers %></li>
                <li>Votes: <%= game.votes%></li>
                </ul> -->
      </div>
      <% } %>
      <div id="userReaction">
        <h4>Tell us how you feel about it:</h4>
        <ion-icon name="happy-outline" size="large"></ion-icon>
        <ion-icon name="sad-outline" size="large"></ion-icon>
      </div>
    </div>
    <h2>And the results are also in regarding the peas!</h2>
    <% console.log(peaStuff)%>
    <h3>People who like peas: <%= peaStuff.peopleWhoLikePeas%></h3>
    <h3>People who dislike peas: <%= peaStuff.peopleWhoDislikePeas%></h3>

    <h2>Next game scheduled:</h2>
    <%- include('./partials/footer.ejs') %>
    <script src="./js/features.js"></script>
  </body>
</html>
